Learn to use git version and keep track of changes (1hr 5m)
branches allow creaters to branch off the main branch to work on their own code
pull request dedicated forum to allow users to discuss changes in a soon to be merged branch
git clone "url" clones a repo
git checkout -b (name) 
git add . (adds all new changes)
git status (tells what changes )
git commit -m "message"
git push --set-upstream origin (branch name)
set up pull request in git hub for team sign off and commits 
then merge 

    git init (new repo)
    git status (inspects the contents of the working directory and staging area)
    git add "." (adds files from the working directory to the staging area [. adds everything])
    git diff (shows the difference between the working directory and the staging area)
    git commit (permanently stores file changes from the staging area in the repo)
    git log (shows a list of all previous commits)